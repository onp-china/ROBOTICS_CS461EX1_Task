# Auto-generated by mydiffusion/scripts/inspect_mimicgen_dataset.py
name: three_piece_assembly_d0_lowdim_abs
manifest_ready: True

obs_dim: 51
action_dim: 10
keypoint_dim: 3

obs_keys: &obs_keys ['object', 'robot0_eef_pos', 'robot0_eef_quat', 'robot0_gripper_qpos']
task_name: &task_name three_piece_assembly_d0
abs_action: &abs_action True
dataset_path: &dataset_path /root/mydiffusion/data/mimicgen/processed/three_piece_assembly_d0/low_dim_abs.hdf5

env_runner:
  _target_: diffusion_policy.env_runner.robomimic_lowdim_runner.RobomimicLowdimRunner
  dataset_path: *dataset_path
  obs_keys: *obs_keys
  n_train: 2
  n_train_vis: 0
  train_start_idx: 0
  # Windows + Mujoco 多进程较吃内存，与 mug/coffee 对齐为稳配置
  n_test: 4
  n_test_vis: 1
  test_start_seed: 100000
  max_steps: 1000
  n_obs_steps: ${n_obs_steps}
  n_action_steps: ${n_action_steps}
  n_latency_steps: ${n_latency_steps}
  render_hw: [128, 128]
  fps: 10
  crf: 22
  past_action: ${past_action_visible}
  abs_action: *abs_action
  # n_envs 太大容易触发 mujoco.FatalError: Could not allocate memory
  n_envs: 2

dataset:
  _target_: diffusion_policy.dataset.robomimic_replay_lowdim_dataset.RobomimicReplayLowdimDataset
  dataset_path: *dataset_path
  horizon: ${horizon}
  pad_before: ${eval:'${n_obs_steps}-1+${n_latency_steps}'}
  pad_after: ${eval:'${n_action_steps}-1'}
  obs_keys: *obs_keys
  abs_action: *abs_action
  use_legacy_normalizer: False
  seed: 42
  val_ratio: 0.02
  max_train_episodes: null
